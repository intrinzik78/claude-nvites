# Architecture Diagram

Visual representation of the system architecture. See [Architecture.md](Architecture.md) for the written form.

```
                    ┌─────────────┐
                    │  QR Code    │
                    │  (physical/ │
                    │   digital)  │
                    └──────┬──────┘
                           │ scan
                           ▼
                    ┌─────────────┐
                    │  nvites.me  │  ← hyper-short URL gateway
                    │  /{code}    │     captures device/location/time
                    └──────┬──────┘
                           │ redirect
                           ▼
                    ┌─────────────┐
                    │ Destination │
                    │   (client   │
                    │    site)    │
                    └─────────────┘

    ┌──────────────────────────────────────────────┐
    │                 API Server                    │
    │  (Rust / Actix-web)                          │
    │                                              │
    │  QR gateway · Analytics · Payments           │
    │  Sessions · Users · Workflows · Secrets      │
    │                                              │
    │  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
    │  │ rate-limit│ │ postmark │ │authorizenet│    │
    │  └──────────┘ └──────────┘ └──────────┘     │
    │  ┌──────────┐ ┌──────────┐                   │
    │  │ qr-frame │ │email-tmpl│                   │
    │  └──────────┘ └──────────┘                   │
    └───────────────────┬──────────────────────────┘
                        │
              ┌─────────┼─────────┐
              ▼         ▼         ▼
        ┌──────────┐ ┌─────┐ ┌────────┐
        │  MySQL   │ │sdk-ts│ │sdk-rust│
        │ (Cloud   │ │     │ │       │
        │  SQL)    │ │     │ │       │
        └──────────┘ └──┬──┘ └───┬───┘
                        │        │
                        ▼        ▼
                   ┌────────┐ ┌──────────────┐
                   │Website │ │Command Center│
                   │(Svelte)│ │  (Tauri)     │
                   └────────┘ └──────────────┘
```
