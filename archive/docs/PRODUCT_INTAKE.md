# Product Intake

Fill in one section per product. Fields marked **(required)** must be provided. Fields with defaults can be left blank to accept the default.

## Reference: Valid Options

**Category:** Paintball (1), Parties (2), GellyBall (3), Corporate (4), Axe Throwing (5), Equipment (6), Paint & Ammo (7), Gift Cards (8)

**Resource Type:** PaintballMarker (0), PartyRoom (1), AxeLane (2)

**Status:** Active (1), Inactive (0) — default: Active

---

## Product 1

- **Name** (required, max 64 chars): Basic
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): basic paintball package for individuals and small groups
- **Price** (required — dollar amount, e.g. $25.00):34.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):1
- **Max Guests** (required — default 1):9
- **Status** (default Active):Active

---

## Product 2

- **Name** (required, max 64 chars): Deluxe
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): deluxe paintball package for individuals and small groups
- **Price** (required — dollar amount, e.g. $25.00):39.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):1
- **Max Guests** (required — default 1):9
- **Status** (default Active):Active

---

## Product 3

- **Name** (required, max 64 chars): Elite
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): elite paintball package for individuals and small groups
- **Price** (required — dollar amount, e.g. $25.00):49.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):1
- **Max Guests** (required — default 1):9
- **Status** (default Active):Active

---

## Product 4

- **Name** (required, max 64 chars): Basic
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): basic paintball package for medium groups
- **Price** (required — dollar amount, e.g. $25.00):29.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):10
- **Max Guests** (required — default 1):29
- **Status** (default Active):Active

---

## Product 5

- **Name** (required, max 64 chars): Deluxe
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): deluxe paintball package for medium groups
- **Price** (required — dollar amount, e.g. $25.00):39.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):10
- **Max Guests** (required — default 1):29
- **Status** (default Active):Active

---

## Product 6

- **Name** (required, max 64 chars): Elite
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): elite paintball package for medium groups
- **Price** (required — dollar amount, e.g. $25.00):49.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):10
- **Max Guests** (required — default 1):29
- **Status** (default Active):Active
- ---

## Product 7

- **Name** (required, max 64 chars): Basic
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): basic paintball package for large groups
- **Price** (required — dollar amount, e.g. $25.00):24.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):30
- **Max Guests** (required — default 1):100
- **Status** (default Active):Active

---

## Product 8

- **Name** (required, max 64 chars): Deluxe
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): deluxe paintball package for large groups
- **Price** (required — dollar amount, e.g. $25.00):34.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):30
- **Max Guests** (required — default 1):100
- **Status** (default Active):Active

---

## Product 9

- **Name** (required, max 64 chars): Elite
- **Category** (required — pick from reference above): paintball
- **Resource Type** (required — pick from reference above): paintball_marker
- **Description** (optional, max 255 chars): elite paintball package for large groups
- **Price** (required — dollar amount, e.g. $25.00):44.95
- **Duration** (required — minutes, e.g. 60):180
- **Min Guests** (required — default 1):30
- **Max Guests** (required — default 1):100
- **Status** (default Active):Active